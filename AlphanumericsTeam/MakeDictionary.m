function entries = MakeDictionary(vector)
% A function for generating a dictionary from an input vector

if(~isvector(vector))
    error('Input should be a vector.');
end

entries = [];
for k = 1 : length(vector)
    
end

